import { TimeDifferencePipePipe } from './time-difference-pipe.pipe';

describe('TimeDifferencePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeDifferencePipePipe();
    expect(pipe).toBeTruthy();
  });
});
