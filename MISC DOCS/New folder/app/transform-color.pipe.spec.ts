import { TransformColorPipe } from './transform-color.pipe';

describe('TransformColorPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformColorPipe();
    expect(pipe).toBeTruthy();
  });
});
